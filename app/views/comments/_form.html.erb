
<%= form_with model: [@message, @message.comments.build] do |form| %>
  <p class="fs-3" > Reply to Message</p>
  <%= form.text_area :content ,:class => "form-control form-control-lg", label: "Comment" %>
   <%= form.button :submit , :class=>"btn btn-primary py-2"%> 
<% end %>  